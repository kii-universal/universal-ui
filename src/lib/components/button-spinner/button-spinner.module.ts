import { CMButtonSpinner } from './button-spinner.directive';
import { NgModule } from '@angular/core';

@NgModule({
  declarations: [CMButtonSpinner],
  imports: [],
  exports: [CMButtonSpinner]
})
export class CMButtonSpinnerModule { }
